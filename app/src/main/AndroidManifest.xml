<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cyndidemo">

    <uses-permission android:name="android.permission.INTERNET" /> <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".messagingActivities.ContactListActivity"></activity>
        <activity android:name=".messagingActivities.ChatActivity" />
        <activity android:name=".messagingActivities.MessagingLoginActivity" />
        <activity android:name=".StudentAllSubjectListActivity" />
        <activity android:name=".StudentEachSubjectActivity" />
        <activity android:name=".subjectActivities.StudentSubjectResourceActivity" />
        <activity android:name=".subjectActivities.StudentSubjectAssignmentActivity" />
        <activity android:name=".subjectActivities.StudentSubjectLiveClassActivity" />
        <activity android:name=".subjectActivities.StudentSubjectClassScheduleActivity" />
        <activity android:name=".subjectActivities.StudentSubjectAssessmentActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>